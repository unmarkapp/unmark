# Demo images

## Try a sample (Clean)

`sample-clean-before.jpg` is the bundled Gemini-style still with a real sparkle in the official 1K bottom-right corner. The web **Try a sample** button loads it into Instant; the iOS app uses the same JPEG from `SampleCleanBefore`.

## Landing before / after

Place matching Gemini photo pairs here (same dimensions):

- `sample-1-before.jpeg` — with Gemini sparkle
- `sample-1-after.png` — cleaned

Optional second pair:

- `sample-2-before.jpeg`
- `sample-2-after.png`

Then add them to `EXAMPLES` in `components/LandingBeforeAfter.tsx`.
